---
project: lpg-store
status: active
spec-layout: folder
last-updated: 2026-06-29
---

# lpg-store

## Description

Backend, frontend dashboard, and chat bot for a small-city LPG (Liquefied Petroleum Gas) tank delivery business. The system tracks the chain of custody from inventory assignment to order fulfillment: which delivery user has which tanks each day, what was sold to whom, when each delivery happened, and how unpaid orders accumulate. **Primary value: traceability**, not analytics or accounting.

The product is pre-production. The backend is currently being rebuilt as v2 (clean reset, with v1 archived under `legacy/`). The frontend and bot are in earlier stages.

## Repositories

| Repo | Path | Stack | Role |
|------|------|-------|------|
| lpg-backend | /home/diegomh/dev/personal/freelance/lpg-store/lpg-backend | Node 22 / Express 5 / Drizzle / Postgres / Redis / TypeScript | backend (v2 in progress) |
| lpg-frontend-vue | /home/diegomh/dev/personal/freelance/lpg-store/lpg-frontend-vue | Vue 3 / Vite / Tailwind / Chart.js / Leaflet / PWA | dashboard frontend — **Piloto** |
| lpg-bot | /home/diegomh/dev/personal/freelance/lpg-store/lpg-bot | Node / Express / axios / commander | chat bot |

**Spec layout: `folder`.** Backend and frontend live in separate repos, so each feature spec is a folder (`specs/{category}/{slug}/`) with a shared `index.md` plus one track file per repo (`backend.md`, `frontend.md`, …). The overall spec is `done` only when every track is done.

## Domains

- [[eng/index]] — architecture, decisions, code patterns
- [[product/index]] — what the product does, user roles, requirements
- [[ops/index]] — deployment, CI/CD, runbooks
- [[specs/index]] — feature specs (draft, approved, in-progress, done)

## Active Work
_No specs in progress — `first-vps-deploy` is done across both tracks; routine deploys run from the per-repo runbooks._

## Recent Changes
| 2026-06-29 | specs/inventory + backend | **`store-history` backend track done → spec `done` (both tracks); the per-store Historial tab is now live end-to-end.** New **`GET /api/v1/inventory/stores/:storeId/history?from=&to=`** — a read-only, **newest-first** merge of the store's **`location`-holder** tank + item ledger rows, each with the **actor's resolved name** (`users` innerJoin), `kind`, `occurredAt`/`businessDate`, the movement (`fullDelta`/`emptyDelta` for tanks, `delta` for items), `notes`, and `refOrderId`/`refCustomerId`. Composed from the **existing** ledgers (`storeHistoryForPeriod` generalises `purchaseLinesForStorePeriod` to all kinds) — **no schema change, no new ledger, no write path**. The **location-holder join is the scope boundary** (a driver-day/assignment leg or a foreign store's rows can't leak in — asserted by test); order events stay **lazy** via the existing `GET /orders?storeId=` (not folded in). Default window = today + 30 prior Lima days; operator own-store / admin-dev, cross-store → **403**. Matches the already-shipped frontend `StoreHistoryEntry` contract (`{ history: [...] }`, non-null `userName`). New `store-history.test.ts` (10). **Shipped together: a pre-existing authz gap fix** — the provider-purchase write endpoints (`POST /stores/:id/{tank,item}-purchases`) enforced only role, letting an operator record a purchase (and its **accounting egress**) into **any** store; now own-store scoped via `resolvePurchaseActor` (caller object → checked, numeric userId → trusted internal/test path), cross-store → 403. Independent validation GREEN (both changes; frontend contract verified). Gates green (typecheck + `biome check` + **148** tests + build). |
| 2026-06-28 | specs/inventory + backend | **`store-stock-adjustments` backend track done → spec `done` (both tracks); the Ajustar-stock dialog is now live end-to-end.** New **`POST /api/v1/inventory/stores/:storeId/adjustments`** sets/corrects a store's standing full/empty stock directly — writes **`adjustment`-kind** `tank_transaction` rows on the store's `location` holder (signed `fullDelta`/`emptyDelta`, **required** note, actor + `occurred_at`), **no driver-day, no purchase, no accounting egress** (`unit_cost` NULL; the registry sums only `kind='purchase'`). One request seeds **multiple types atomically**, so a brand-new store goes 0 → opening stock in one call; a **validate-all-before-write** pass rejects an unknown type (**400**) or a would-go-negative result (**409**, names the type) before any insert, so a multi-line request stays atomic even on the no-rollback test fake. Scoping mirrors the purchase surfaces (own-store operator + admin/dev; cross-store → **403**) — note `recordTankPurchase` itself never enforced own-store, so the explicit check follows the `listStorePurchases` pattern. Reuses the existing `adjustment` kind + `location` holder — **no schema/migration change**. New `store-stock-adjustments.test.ts` (15 tests). Independent validation GREEN (all 7 backend criteria). Gates green (typecheck + `biome check` + **134** tests + build). |
| 2026-06-28 | specs/inventory + frontend (Piloto) | **`store-stock-adjustments` + `store-history` frontend tracks done (both backends pending in lpg-backend).** Two capabilities added to the per-store detail `/inventario/tiendas/:id`. **Ajustar stock** (store-stock-adjustments): a header button → `StockAdjustmentDialog` to set/correct a store's standing tank stock in one step — **Establecer total** or **Ajustar (±)** per type, required Motivo (Carga inicial / Conteo físico / Merma / Otro), negative-balance guard; **seeds an empty store from 0** in one save; `POST /inventory/stores/:id/adjustments`. **Historial tab** (store-history): a per-store **movement book** (purchase / opening / load / sale / return / **ajuste** / carry, each with **actor + time + note** + an order link), **lazy on open**, windowed (default last 30 days); `GET /inventory/stores/:id/history`. **Order events stay lazy** behind a "Ver eventos de pedidos" button reusing `GET /orders?storeId=` (no new endpoint). Independent validation: all criteria pass, no bugs. Gates green (typecheck + build). **Both backend tracks remain** — the inventory plan is now frontend-complete; two read/write endpoints to add in lpg-backend. |
| 2026-06-28 | specs/inventory + both repos | **`store-detail-products` done (both tracks).** The per-store detail `/inventario/tiendas/:id` is now purchased-only + item-aware + tabbed (frontend details in the 2026-06-27 entry). **Backend (lpg-backend, by owner):** the contracted **`GET /inventory/stores/:storeId/item-availability`** is live — `{ items: [{ inventoryItemId, qty }] }` from the `item_balance` view (`kind='location'`), **holder-based** (a purchased-then-0 item shows at `qty 0`; never-purchased is absent), via repo `itemBalancesByLocation` + service `getLocationItemAvailability`/`toItemRows`. The frontend **Artículos** tab now shows real per-store item stock. Backend gates green (typecheck + biome + 119 tests + build); contract verified from the frontend. |
| 2026-06-28 | specs/inventory + backend | **`store-detail-products` backend track done → spec `done` (both tracks); the Artículos tab is now live end-to-end.** New **`GET /api/v1/inventory/stores/:storeId/item-availability`** → `{ items: [{ inventoryItemId, qty }] }` from the `item_balance` view filtered to `kind='location'` — **holder-based**, mirroring the tank `getLocationAvailability`: a never-purchased item is **absent**, a purchased-then-0 item shows at `qty: 0` (verified against the `.existing()` view SQL in migration `0002`: `item_holders LEFT JOIN item_transactions GROUP BY h.id`, `COALESCE(SUM(delta),0)`, **no `HAVING`**). Repo `itemBalancesByLocation` + service `getLocationItemAvailability`/`toItemRows` (refactored the Fake to share `sumItemHolder`); auth mirrors the tank availability route (authenticated, no extra guard). New `store-item-availability.test.ts` (6 tests). Independent validation: both backend criteria MET. Gates green (typecheck + `biome check` + **119** tests + build). |
| 2026-06-27 | specs/inventory + frontend (Piloto) | **`store-detail-products` frontend track done (backend track remains).** Per-store detail `/inventario/tiendas/:id` reworked: **tank stock is now holder-based** — only products the store has **already purchased** show (incl. those depleted to 0); never-purchased types are **hidden** (owner's rule). The page split into **Balones · Artículos · Compras** tabs with **Artículos + Compras lazy-loaded** on first open. The new **Artículos** tab consumes a new **`GET /inventory/stores/:storeId/item-availability`** (location item balances, holder-based) — **backend track pending in lpg-backend** (the tab shows empty until it ships). **Follow-on (orders, user-approved):** `/pedidos/nuevo` step 2 (`OrderLinesEditor`) reworked into **Balones/Accesorios category tabs + compact one-line rows in a capped-height scroll** (the price field reveals only for a selected product) plus a running count/total, so a large catalog no longer pushes the *Siguiente* button out of reach. Gates green (typecheck + build, PWA 76 entries). |
| 2026-06-27 | specs/inventory + both repos | **`store-stock-first` done (both tracks) — multi-location inventory overview.** `/inventario` now leads with a **Resumen** tab: one card per store the user oversees (operator → their stores, admin/dev → all) showing Llenos/Vacíos totals + per-type + En vehículos + today's driver/day-state, **never-blank** (a 0-stock store reads 0/0, not a blank — the reported "Stock en tienda empty" symptom). Tabs reordered to **[Resumen · Asignaciones · Por consolidar]**; the old **Disponibilidad** tab retired into a per-store detail route **`/inventario/tiendas/:id`** (catalog-merged never-blank stock table + on-truck + provider purchases/cost-correction). **Backend (lpg-backend, by owner):** new caller-scoped aggregate **`GET /inventory/availability`** → `{ stores: [{ storeId, storeName, shop[], onTruck[] }] }` (global → all active stores, operator → `storeIdsForUser`, `?storeId` intersect; grouped reads; a no-holder store still appears). **Frontend (Piloto):** `getStoresAvailability` + store `overview`/`todayAssignments` (today decoupled from the Asignaciones filter), new `StoreDetailView`, removed orphaned persisted `selectedStoreId`. Independent validation: all 6 frontend criteria MET; gates green (typecheck + build, PWA 76 entries). Manual on-device smoke left to the operator. |
| 2026-06-24 | specs/deployment + frontend (Piloto) | **`first-vps-deploy` frontend track done → spec `done` (both tracks); the product is deploy-ready end-to-end.** Piloto now ships as its **own `nginx:alpine` image behind the shared Traefik edge**, deploying independently of the backend. New `docker/Dockerfile` (multi-stage: `node:22-alpine` → `npm run build` → nginx serving `dist/`), `docker/nginx.conf`, `docker/docker-compose.yml` (one `frontend` service on the external `lpg-edge` only; Traefik labels mirror the backend exactly — `Host(${APP_HOST})`, `websecure`, `tls=true`, resolver `le`, `secure-headers@file`, LB port 80), `docker/.env.prod.example`, a net-new GHCR-publish CI (`.github/workflows/main.yml`, no VPS secrets), `.dockerignore`, and `docs/DEPLOYMENT.md`. **Owner decisions:** (1) **same-origin `/api`** — nginx reverse-proxies `/api`→`api:3000` over `lpg-edge` (Docker resolver + variable upstream so it boots without the backend; forwards `/api/v1/...` verbatim), so the build keeps `VITE_API_URL=/api` exactly as in dev — **no CORS, no mixed-content, zero backend changes**; (2) **alpine over distroless** for the MVP (one-line swap later). nginx also does SPA history fallback, gzip, immutable cache on hashed `/assets/`, and no-cache on `index.html`/`sw.js`/`push-sw.js`/`registerSW.js`/`manifest.webmanifest` (`application/manifest+json`). VITE_* (incl. the publishable VAPID key) bake in as **build args** in CI. Removed the stale Netlify `public/_redirects`; no source changes. Gates green: typecheck + build, `docker compose config`, `docker build`, container smoke (deep links, cache headers, `/api`→502 without backend = proxy wired). Independent validation: **all 6 frontend + cross-cutting criteria MET**. The first deploy itself stays a manual owner action per the runbooks. |
| 2026-06-18 | specs/stores + frontend (Piloto) | **`catalog-item-management` frontend track done → spec `done` (both tracks).** Admin **edit + activate/deactivate** for the two product catalog tables, closing the create-only parity gap from the 2026-06-18 v1 audit. The Tipos de tanque / Artículos tabs gained per-row **Editar**: the create dialogs became dual create/edit (`TankTypeFormDialog`/`ItemFormDialog`, prefilled on open, `active` Switch shown only in edit), mirroring the Tiendas `StoreFormDialog`; show-inactive toggle + Estado badges already existed. New `updateTankType`/`updateItem` service+store methods (`PATCH /catalog/tank-types/:id` + `/items/:id`); client validation mirrors backend Zod (`sizeLabel`/`description` blank → `null` clears; item `unit` is NOT NULL → omitted when blank, never null). Design-system tokens only, ≥44px targets. Independent validation: all 6 shared criteria PASS, no bugs/violations. Gates green (typecheck + build, PWA 73 entries / 772.25 KiB). Manual both-theme + 390px smoke left to the operator. |
| 2026-06-18 | specs/ui-design + frontend (Piloto) | **Follow-on order-detail UX refinements (user-approved, `mobile-layout-audit` stays `done`).** On the shared `OrderDetailView` (`/pedidos/:id` + `/mis-entregas/:id`): **logout moved from the top bar into the drawer** (pinned bottom); the run-on "date · método · tienda" line became an **icon-labelled metadata card**; the contextual actions moved into a **floating `sticky bottom-0` action bar** (always thumb-reachable on a phone) with **icon + plain-language labels that name the action** (Despachar → "Empezar despacho", Entregar → "Confirmar entrega", Falló → "No se pudo entregar", etc.); and the **`Historial` timeline node** text was restructured into distinguishable **icon + label + value** rows (User "Por" / UserCheck "Para" / CreditCard method, Motivo in a tinted box). Tokens only, no behavior/data change. Gates green (typecheck + build). |
| 2026-06-17 | specs/ui-design + frontend (Piloto) | **`mobile-layout-audit` done (frontend track) → spec `done`; 2nd `ui-design` spec.** The `design-system` pass styled colors/type but never audited **layout**, so 14/19 views overflowed on a ~390px phone (the app is a field PWA — phone is the primary case). New shared **`ResponsiveTable`** (`components/app/`) — a real `<table>` at `sm+` and a **stacked label/value card per row below `sm`** (composes the pristine `ui/table/*`; owns Spinner-loading + empty states via `:loading`/`empty-text`) — replaced **all 17 raw data tables across 10 views** (OrderDetailView, AssignmentDetailView, InventoryView, CatalogView, OrdersListView, AccountingListView, CustomersListView, OrganizationView, CustomerDetailView, DriverDayView), fixing the reported `OrderDetailView` `Vacíos`-clip + inner-scroll. **Chrome:** `TabsList` → horizontal-scroll strip (`flex w-max max-w-full overflow-x-auto` + `TabsTrigger shrink-0`) so no tab clips (the reported "Todas" clip); `AppLayout` top-bar de-cram (icon-only logout `<sm` + `aria-label`, user name hidden `<sm` and moved to a drawer identity block, `px-4 sm:px-6`). **Filter bars** → `w-full md:w-…` (Orders/Accounting/Customers/Inventory). Conditional columns (Orders/Accounting `Tienda`) + action clusters preserved verbatim via `#actions`; Inventory whole-row clicks → explicit "Ver" buttons (now matches the design-system row-link rule); CustomerDetail totals rows kept outside the table. **Layout/markup only — no token/behavior/copy change.** `eng/design-system.md` updated (§5 `ResponsiveTable` canonical + phone-first layout bullet, hard rule #11). Independent validation: all in-repo criteria PASS, no missed `<Table>`. Gates green (typecheck + build, PWA 73 entries / 764.51 KiB). Manual on-device 390px both-theme smoke left to the operator. |
| 2026-06-16 | specs/notifications + backend | **`push-notifications` backend track done — first `notifications` spec.** Native **Web Push + VAPID** (no Firebase). New `notifications` vertical module: `push_subscriptions` table (migration `0013`, **unique `endpoint`**, FK→users `on delete cascade`) with `POST`/`DELETE /api/v1/notifications/subscriptions` (auth'd, **upsert by endpoint** → idempotent re-subscribe + shared-device re-bind; always bound to the caller, never a body field) and `GET /vapid-public-key` (returns only the **public** key). `NotificationsService.notifyUser` fans out via the `web-push` library, **prunes** `404`/`410` Gone endpoints, and is **best-effort** (never throws into the caller). The two triggers fire through an **injected `DriverNotifier` port** (ADR-012 late-bind in `app.ts`, mirroring `setAccountedPeriodGuard`) so neither inventory nor orders imports the notifications module: `inventory.openDay` → "Nueva asignación del día" → `/mi-dia`; `orders.assignOrder` → "Nuevo pedido para entregar" → `/mis-entregas/:id` (**not** on dispatch). Both fire **after commit** so a push failure can't roll back the day-open/assign. **The whole point — zero client secrets:** only the **public** VAPID key is publishable; `VAPID_PRIVATE_KEY` is backend-env-only, never logged or returned. Design seam: an injectable `PushSender` (default wraps `web-push`) doubles as the **no-op send mode** when VAPID env is absent + the test double, so `npm run dev` and `node --test` need no keys or network. **100 tests** (+4 lifecycle: upsert idempotent, 410 prune, empty no-op, trigger fires the port). Gates green (typecheck + `biome check` + 100 tests + build). Independent validation: all 7 backend criteria + 4 risk checks PASS, no gaps. Frontend track remains. |
| 2026-06-16 | specs/notifications + both repos | **`push-notifications` done (both tracks) → first `notifications` spec; the v1 Firebase/FCM dependency is retired.** Native **Web Push + VAPID** PWA notifications so a **delivery driver** gets a push for a **new daily assignment** (→ `/mi-dia`) or a **new delivery job** (→ `/mis-entregas/:id`) — **no Firebase, and zero secrets in the client build by construction** (the client carries only the publishable `VITE_VAPID_PUBLIC_KEY`; the private key lives solely in the backend env). This directly answered the owner's ask — the v1 build-time config-injection ceremony was treating publishable Firebase identifiers as secrets; native Web Push removes the question entirely. **Backend (verified this session, implemented prior):** new `notifications` module (`push_subscriptions` keyed on unique `endpoint`, migration **0013**), `web-push`, `POST`/`DELETE /api/v1/notifications/subscriptions` (caller-bound upsert / idempotent delete) + `GET /vapid-public-key`; best-effort `notifyUser` that fans out and **prunes 404/410 Gone**; **no-op send mode** without VAPID; triggers wired in `app.ts` via **ADR-012 injected `DriverNotifier` ports** (inventory day-open + orders assign, after-commit, never block the business action); 4 tests. **Frontend (Piloto):** new `notifications` vertical module (service + Pinia store, provide-pattern), a **delivery-only, gesture-safe "Activar notificaciones"** card on the delivery home (`pushManager.subscribe` w/ base64url→Uint8Array key → POST; four states incl. iOS-needs-install + denied-guidance; turn-off = local `unsubscribe` + DELETE), `public/push-sw.js` (`push` → `showNotification`; `notificationclick` → focus/navigate-or-openWindow; `pushsubscriptionchange` → re-subscribe) imported into the Workbox SW via `workbox.importScripts` (existing precache + `/v1/` NetworkFirst intact), and **disable-on-logout** in `AppLayout`. `.env.example` stripped of the stale Firebase block. Independent validation: **all 6 criteria met, no blocking bugs**, design-system clean. Gates green (typecheck + build, PWA 69 entries / 777 KiB); **verified no Firebase/FCM/private-key in `dist/`**. Manual on-device end-to-end smoke (needs the backend running with a real VAPID pair) left to the operator. |
| 2026-06-16 | specs/inventory + frontend (Piloto) | **`stale-day-banner` frontend track done → spec `done` (single track); new `inventory` spec.** Persistent **"Días anteriores sin resolver"** banner above the `/inventario` tabs surfacing **unresolved prior days** — non-`carried` assignments dated before today (stuck `open` + counted-but-unconsolidated `closed`) — so office staff clear them before opening today. Previously these were hidden: the Asignaciones tab defaults its date filter to today, and "Por consolidar" only lists `closed` (incl. today's), never stuck-`open`. **Frontend-only — no backend change** (the user-confirmed decision): `store.ts` adds `fetchStaleOpenDays()` (`GET /assignments?state=open`, caller-scoped) filtered `date < todayISO()`, merged via a computed `stalePriorDays` with the past-dated slice of the already-fetched `closed` consolidation queue (no extra request), oldest-first. Each row (date · driver/store via the catalog map · state `Badge` · action hint: closed→"Lista para consolidar", open→"Cerrar (forzar) y consolidar") links to the assignment detail where the existing Consolidar / Forzar cierre resolve it; banner re-fetches on remount so resolved days drop off. Reused the design system — added a shared `Alert` **`warning`** variant (tokens only, mirroring `destructive`); rows ≥44px. `service.ts` untouched. Independent validation: all 8 criteria PASS, no correctness bugs. Gates green (typecheck + build, PWA precache 68 entries). Manual smoke left to the operator. |
| 2026-06-16 | specs/accounting + frontend (Piloto) | **`registry-source-drilldown` frontend track done → spec `done` (both tracks).** Read-only per-block, day-grouped **verification drill-down** on the registry detail, consuming the backend's `GET /accounting/registries/:id/lines` sub-resource (frontend `types`/`service`/`store` + new presentational `RegistryDrilldownDays.vue`). Each **Ingresos/Egresos** card got its **own expand toggle** ("Ver detalle por día"): an expanded card spans the full grid row (`lg:col-span-2`), pushing the other card below — independent, so **both can be open at once** (per the user's refinement). The existing aggregate summary stays visible; the drill-down renders beneath the block total as a **phone-first stacked card per business day** (date + day subtotal over the day's orders/purchases) with a **"Suma del detalle"** row echoing `operationalTotal`/`providerTotal` (the fields the subtotals reconcile to — not the manual-inclusive `total`). **One lazy fetch backs both cards** (single endpoint, keyed on `drilldownFor` so a different registry can't leak its lines); **closed registries serve the frozen snapshot via the same call — no status branch, no edit affordances**. Also (user note): redesigned the store/period data section from a plain `<p>` grid into an **icon-labelled metadata `Card`** (Tienda/Periodo/Notas/Cerrado el). Design tokens only; reuses `Spinner`/`formatMoney`. Independent validation: all 5 frontend criteria MET, no correctness bugs. Gates green (typecheck + build). Manual smoke (expand a block → day subtotals add to the block total) left to the operator. |
| 2026-06-16 | specs/inventory + frontend (Piloto) | **`provider-purchase-cost` frontend track done → spec `done` (both tracks).** Operators now enter the **provider cost-in** when recording purchases and can **correct** it after the fact. **Purchase dialog** gains a **required**, catalog-`purchasePrice`-prefilled **"Costo del proveedor"** field on **both** tank and item lines (helper text + `formatMoney` sell-price reference make it read as cost, not sell price), validated like the backend Zod (`≥0`, ≤2dp) and sent as `unitCost`. **Availability** tab gains a **"Compras al proveedor"** windowed list (new `GET /inventory/stores/:id/purchases`, user-enabled in backend) with an **"Editar costo"** affordance → `CostCorrectionDialog` (`PATCH /inventory/purchases/:kind/:id/cost`) that refetches so an **open** registry's egress moves live; rows in a **closed** registry (`accounted`) show a frozen **lock**. `AssignDialog` "Comprar y cargar" left untouched (no cost → catalog default). Independent validation: all 5 frontend criteria met, no blocking bugs, design-system clean. Gates green (typecheck + build, PWA precache 68 entries). Manual cost-below-sell smoke left to the operator. |
| 2026-06-16 | specs/inventory + backend | **`provider-purchase-cost` backend track done.** Editable provider **cost-in** captured per purchase so accounting **egress** stops valuing restocks at the *sell* price. Migration `0012` adds nullable `unit_cost` to `tank_transactions` + `item_transactions` (NULL for non-purchase / legacy rows). Tank/item purchase requests gain an **optional** per-line `unitCost`; the service defaults it to the **last cost paid** for that store+product, else the catalog `purchase_price` (catalog **not** mutated). Egress now `COALESCE(tx.unit_cost, holder.purchase_price) × qty` in both the Drizzle and batched raw-SQL ports (legacy NULL rows still value at the holder snapshot — smoke-verified on dev DB). New **`PATCH /inventory/purchases/:kind/:id/cost`** correction endpoint (operator own-store + admin/dev; rejects non-purchase rows + cross-store) — a correction moves an **open** registry's egress live, but a purchase whose day is **accounted** (covered by a closed registry) is **frozen → 409** for every role (injected accounting `isStorePeriodAccounted` guard, wired in `app.ts` so inventory never imports accounting; no dependency cycle). Also a **`GET /inventory/stores/:storeId/purchases?from=&to=`** list endpoint (the data behind the edit-cost UI — none existed; the shared `purchaseLinesForStorePeriod` read is reused by the future drill-down). **95 tests total** (8 new in `provider-purchase-cost.test.ts`); fakes extended faithfully. Gates green (typecheck + `biome check` + 95 tests + build). Independent validation GREEN, all backend criteria. Frontend track remains. |
| 2026-06-15 | specs/inventory + frontend (Piloto) | **`multi-type-fulfillment` frontend track done → spec `done` (both tracks).** Display/flow-only slice over `inventory-foundation`, no new endpoints. **Smart "Comprar y cargar"** in the orders `AssignDialog`: on entering the oversell-warnings phase it snapshots the order store's full-tank stock (`getShopBalances`) into a `Map`; `resolveShortfalls()` (replacing `loadMissingTanks`) per shortfall **loads** straight when the store has the fulls, else registers a provider **purchase** of only the missing part (`shortfall − have`, no double-buy) **then loads** the full shortfall — so a driver-and-store stockout (e.g. 45kg) is resolved end-to-end in one action; a `needsPurchase` computed flips the button "Cargar balones faltantes"→**"Comprar y cargar"** and shows a Compra note; errors surface the backend's now tank-type-named 409. **Per-type Movimientos legibility** in `AssignmentDetailView`: new **Tanque** column (resolves `tankTypeId`→name via the catalog resolver, colspan 5→6) + `KIND_LABELS.load = "Carga"`. **Types:** `"load"` added to `TX_KINDS` (all 8 backend kinds; exhaustive `Record<TxKind,string>` would fail typecheck on a gap) + `tankTypeId` on `TankTransaction` (already on the wire). No cross-module type import (orders caches stock as a plain `Map`). Independent validation: all 3 frontend criteria met, no correctness bugs. Gates green (typecheck + build). Manual multi-type smoke (driver+store both lack 45kg → Comprar y cargar → deliver) left to the operator. |
| 2026-06-15 | specs/accounting + frontend (Piloto) | **`accounting-registry` frontend track done → spec `done` (both tracks); first `accounting` spec complete.** New `accounting` vertical module (Piloto) consuming the registry API. **List**: per-store table with status filter, admin/dev **store switcher** + operator own-store scope banner, and **net/ingresos/egresos** columns from the backend's `RegistrySummary` totals (no N+1; `null`→"—"); **"Nuevo registro"** dialog (role-scoped store select, Lima `DatePicker` window, overlap **409** surfaced, routes to the new detail). **Detail**: Ingresos card (operational by payment method + manual) and Egresos card (provider tanks/items/recargos + manual) via `formatMoney`, **manual add/remove only while `open`** (`ManualEntryDialog` — direction, amount >0 ≤2dp, direction-aware category presets + "Otro" free text, optional ingress method, note), prominent **Neto**, admin-only **Cerrar registro** confirm → **read-only frozen** state (closed-at + "instantánea congelada" notice). "Contabilidad" nav added for admin/developer/operator. Design-system: tokens only (`text-success-text`/`text-destructive-text`), canonical status→variant (open→info, closed→secondary) recorded in `eng/design-system.md` first. Independent validation: all 5 frontend criteria + design-system rules met, no gaps. Gates green (typecheck + build). Manual both-theme smoke left to the operator. |
| 2026-06-15 | specs/accounting + backend | **`accounting-registry` backend track done — first `accounting` spec.** New `accounting` vertical module: a **per-store closing register** for the money lifecycle. `accounting_registries` (Lima business-date window, `open→closed`, **non-overlapping per store** → 409) + `accounting_entries` (free-form `ingress`/`egress` manual lines, open-only). The registry **breakdown** composes existing ledgers via ADR-012 read ports — **ingress** = delivered-order `order_payments` grouped by method (keyed by the **payment's** business date) from `OrdersService.paymentsByMethodForStorePeriod`; **egress** = provider purchases (`kind='purchase'` valued `purchase_price × qty` + `purchase_surcharges`) from `InventoryService.purchaseCostsForStorePeriod` — no duplicated queries. **Close freezes a `snapshot`** so later payments/purchases never move a closed period (verified by test). **Scoping:** admin/dev global; operators manage/view only their `storeIdsForUser` stores; **close is admin/dev-only**; `?storeId` intersects scope (un-widenable). Also **wired item-holder price snapshot** in inventory (was hardcoded `0.00` "until the reports spec") so item egress isn't silently zero. Migration `0011` (applied). New `accounting.test.ts` (lifecycle/overlap/scoping) → **84 tests** (was 81). Independent validation: all 7 backend criteria MET. Gates green (typecheck + lint + 84 tests + build). Frontend track remains. |
| 2026-06-15 | specs/orders + both repos | **`order-event-timeline` done (both tracks).** The order detail (`/pedidos/:id` + driver `mis-entregas/:id`) now shows **one full-width horizontal timeline** merging status changes and payments by datetime, each node naming the actor(s) involved. **Backend:** the `GET /orders/:id` detail is enriched with role-safe actor **names** — `statusHistoryForOrder` joins `users` (actor `innerJoin` + new target `leftJoin`) for `changedByName`/`targetUserName`, `paymentsForOrder` joins for `recordedByName` (the detail is served to non-admin roles, so names can't be resolved client-side via the admin-only `/users`). A new nullable `order_status_history.target_user_id` (migration **0010**, applied) captures the *directed* target of an event; `assignOrder` resolves the driver via a new `InventoryService.getStoreAssignment` and stores it, so the **Asignado** node reads operator → driver. New enriched repo return types + view fields; +1 test (**81 total**). **Frontend (Piloto):** new `OrderTimeline.vue` (merge + chronological sort, status & payment node variants) replaced the two-card `lg:grid-cols-2` (Historial + Pagos); deleted the superseded `StatusTimeline.vue`; `types.ts` mirrors the new fields. Independent validation green both tracks. Gates green both repos (backend typecheck+lint+81 tests+build; frontend typecheck+build). Manual phone visual check left to the operator. |
| 2026-06-15 | specs/inventory + frontend (Piloto) | **`day-handoff` frontend track done → spec `done` (both tracks).** Two-signature day-end layered on `inventory-foundation` (no new module). **Driver (1st signature):** new delivery-scoped `/mi-dia` → phone-first `DriverDayView` loads the driver's today assignment (caller-scoped `getMyDay`) + new `CountDialog` — a **mandatory** physical count per tank type (expected-vs-entered + live discrepancy preview, completeness mirroring the backend close gate) → `close`; closed state shows “esperando consolidación” (no carry). Added “Mi día” to the delivery nav + DeliveryHome card. **Operator (2nd signature):** InventoryView gains a **“Por consolidar”** tab (badge count) listing `state=closed` (new caller-scoped `consolidationList`) → detail's existing discrepancies + **Consolidar**/`carry`. **Role gating mirrors backend:** AssignmentDetailView's close is now admin/dev-only, relabelled **“Forzar cierre”** (operators never close); carry stays operator/admin. **Open-day carry-forward + stale guard:** OpenDayDialog pre-fills the opening from `GET /suggested-opening` (envelope `tanks`, fully editable) and, on driver select, surfaces the driver's **pending** non-`carried` days with per-day resolve links + blocks submit (mirrors the openDay 409, whose Spanish message also surfaces via the structured error envelope). New type `SuggestedOpeningResponse`. Gates green (typecheck + build, PWA precache 64 entries / 712.72 KiB); independent validation — all frontend criteria + both client-side guards met. Manual two-party smoke (open→count→consolidate→next-day carry-forward) left to the operator. |
| 2026-06-15 | specs/admin + frontend (Piloto) | **`org-management` frontend track done → spec `done` (both tracks); first `admin` spec complete.** New admin/developer-only **Organización** view (`/organizacion`) consolidating the scattered people-and-places surfaces into one location-centric board. A new `organization` vertical module **composes** `catalog` + `users` + `auth` (no duplication, per v2 conventions): the board renders one `Card` per store from the aggregate `GET /catalog/stores-with-assignments`, grouped **Operadores**/**Repartidores** with per-store **Asignar**/**Quitar** (assignment-id resolved from the catalog's active assignment list, since the aggregate omits it), **Editar** + **Nueva tienda** (reused `StoreFormDialog`/`StoreAssignmentDialog`, the latter gaining an optional `lockedStore` prop), and a show-inactive toggle. A **Usuarios** card lists all users with **Global** / **Sin tienda** flags, inline **Editar** (`UserEditDialog`), and **Invitar usuario** — a new dialog POSTing `/auth/register` (new `auth` `register` service+store action) that surfaces a **copyable `/invite/:token` link** + Lima-time expiry (no email infra in MVP). Old surfaces **replaced**: `AppLayout`/`AdminHome` Usuarios → Organización, Catálogo lost the *Tiendas*/*Asignaciones* tabs (keeps Tipos de tanque + Artículos), `/usuarios` → `/organizacion` redirect (`UsersListView` deleted; users store/service retained). The three reused dialogs emit `saved` so the board refreshes. Gates green (typecheck + build); independent validation met all 4 frontend criteria. Manual both-theme smoke left to the operator. |
| 2026-06-14 | specs/orders + frontend (Piloto) | **`orders-queue-history-search` frontend track done → spec `done` (both tracks).** `/pedidos` reshaped around its two jobs: an **Activos**/**Historial** segmented control (shared `Tabs`) — Activos (default) is the live, naturally-bounded queue (pending/assigned/in_transit, no date); Historial is the finished set (delivered/failed/cancelled) retrieved by a **Desde/Hasta** shared-`DatePicker` range (default today, no pager). Debounced (≥2-char) customer **search** box in both modes → `GET /orders?search=`, **accent-insensitive end-to-end** (consumes the backend `unaccent` fix; the order-creation picker gets “Maria”⇄“María” for free). Backend `status` is a single enum, so the “Todos”-within-mode view **fans out** one request per status in the store and merges by `createdAt`/`id` desc. Admin store switcher + Tienda column + ownership/transfer affordances all preserved; one batched watcher → single re-fetch per change; `onMounted` seeds Activos (never a no-status fetch of the unbounded history). Gates green (frontend typecheck + build); independent validation met all 5 criteria. Manual accent smoke left to the operator. |
| 2026-06-14 | specs/orders + frontend (Piloto) | **Follow-on to `orders-multi-location` (user-approved): operator location visibility & defaults.** Shared `catalog` foundation: `listStoreAssignments({userId})` + `myStores`/`fetchMyStores`. **`/pedidos/nuevo`** — owning-branch selector now shown to operators, scoped to their stores, pre-selected to the first (disabled if single); zero-store operator blocked with a clear message. **`/pedidos`** — operator scope line + Tienda column for multi-store operators. **`/inventario` Disponibilidad** — picker limited to the operator's stores, defaults to the first. Admin/dev unchanged. Gates green (frontend typecheck+build); independent validation met all 3 asks. _(Backend availability endpoint not yet role-scoped — client-side only; candidate follow-up.)_ |
| 2026-06-14 | specs/orders + both repos | **Follow-on to `orders-multi-location` (user-approved): branch-scoped the driver-assignment picker.** `GET /inventory/assignments` was unscoped — any caller saw every store's open days, so admin/the assign dialog could assign a driver from another branch. Backend: new `listAssignmentsForCaller(query, caller)` mirroring the orders-queue scoping — delivery → own days only; operator → their branch(es) (**multi-location aware** via `storeIdsForUser`); admin/dev global; `?storeId=` **intersected** with the caller's scope (tampered id → 0 rows, never a widening grant). 8 scoping tests → **70 total**. Frontend: `AssignDialog` takes the order's `storeId` and fetches the picker scoped to the order's location (both call sites pass it). Gates green both repos (backend typecheck+lint+build+70 tests; frontend typecheck+build). Documented in the `orders-multi-location` track notes (spec stays `done`). |
| 2026-06-14 | specs/stores + frontend (Piloto) | **`store-management` frontend track done → spec `done` (both tracks).** Admin write surface completed on the `catalog` module — no new routes/nav (Catálogo already admin-gated). New catalog store/service/types methods over the backend's `POST`/`PATCH /catalog/stores` + `/catalog/store-assignments`. **Tiendas** tab gains a `StoreFormDialog` (create + edit; name ≤120 / optional address ≤255 / phone ≤32 mirroring backend Zod; **Activa** Switch = activate/deactivate; show-inactive). New **Asignaciones** tab + `StoreAssignmentDialog`: user (reuses `useUsersStore`, filtered to active operator/delivery) + store pickers, list (who · rol · tienda · estado), per-row **Desactivar**. 409s (dup store name / dup active assignment) surface the backend Spanish message via the existing `ConflictError` mapping — no extra wiring. New stores flow straight into the orders store switcher / TransferDialog (shared `catalog.stores`). Independent validation: all 3 frontend criteria PASS. Gates green (typecheck + build, PWA precache 53 entries / 673.80 KiB). Manual both-theme smoke left to the operator. |
| 2026-06-14 | specs/orders + frontend (Piloto) | **`orders-multi-location` frontend track done → spec `done` (both tracks).** Additive changes to the `orders` module: `storeId`/`ownerId` on summaries; admin **branch switcher** (`?storeId=`, server-side filter) + **Tienda column** in the all-branches view; **`OwnershipBadge`** (Tú / Otro operador / Sin asignar) with assign/cancel/transfer **gated to mirror the backend** (owner-or-admin; assign auto-claims when unowned; field actions stay un-gated); **`TransferDialog`** (`POST /:id/transfer`, pre-dispatch, routes back to the queue after handoff); admin **required owning-branch selector** on create (operators default server-side); **409 concurrent-edit** re-sync (`resyncOnConflict` keeps the friendly message + re-fetches). Cross-repo follow-on (user-approved): added a `storeId` query param to backend `GET /orders` (intersected with the caller's visible stores) so the switcher filters server-side — backend 58 → **59 tests**. Gates green both repos (frontend typecheck+build; backend typecheck+lint+build). |
| 2026-06-14 | specs/orders + eng | **`orders-multi-location` backend track done.** Location + ownership dimension on orders: `orders.storeId` (NOT NULL) + `ownerId` (migration `0008`, back-fill to the seeded store); operator branch-scoping via `store_assignments` (cross-branch detail → 404; admin/dev global; delivery by assignment); owner-gated `assign`/`cancel`/`transfer` with auto-claim of unowned orders on assign; new `POST /:id/transfer` branch handoff (pre-dispatch; re-homes store, clears assignment, reverts to pending, releases owner). **Atomic transitions** — every state move is a compare-and-swap conditional `UPDATE … WHERE status=<expected>` (0 rows → 409), closing the lost-update assign race; deliver's flip is the first write in the tx (**ADR-016**). Payments branch-scoped (not owner-gated). 4 new tests → 58 total; typecheck+lint+build green; migration applied. Frontend track remains. |
| 2026-06-12 | specs/ui-design + frontend (Piloto) | **`design-system` done (frontend track) → spec `done`; first `ui-design` spec.** Full UI overhaul of Piloto for the low-tech-literacy operator/driver/admin audience. **Palette** iterated live with the owner to **"petrol + flame"**: deep petrol primary (white text both themes), cool petrol-tinted neutral surfaces, flame orange (`--brand`) reserved for the wordmark + focus ring; semantic `success/warning/info/destructive` fills plus text-grade `*-text` shades for dark-mode legibility — light + dark both first-class. **Foundation:** bundled self-hosted **Public Sans** (`@fontsource-variable/public-sans`, PWA-precached), global type-scale floor (xs→14px, sm→15px), `h-11 md:h-10` ≥44px touch sizing, comfortable table density. **Theme system:** `src/lib/theme.ts` (vueuse `useColorMode`, key `piloto-theme`, system default), FOUC guard + theme-color metas in `index.html`, Sun/Moon toggle in shell + login; removed the hardcoded dark default. **Shared chrome:** `components/app/{PageHeader,Spinner,EmptyState}` + `lib/format.formatMoney` (killed the per-module `money()` dupes); `TableEmpty`+`Spinner` for every table state. **Retrofit** of the app shell + all 7 modules with the canonical status→badge mapping; **DeliveryListView** rebuilt as a phone-first **Card list** (full address, `tel:` link, full-width actions). **`eng/design-system.md`** written as the canonical convention doc + hard-rule checklist for all future UI work. Independent validation: **all 10 acceptance criteria PASS**, zero raw palette classes. Gates green (typecheck + build, precache 656 KiB). Manual both-theme smoke left to the operator. |
| Date | Domain | Summary |
|------|--------|---------|
| 2026-06-10 | specs/orders + frontend (Piloto) | **`orders-foundation` done (both tracks) → spec `done`; the v2 core business workflow is complete.** Frontend `orders` vertical module (Piloto): operator entry **wizard** (Cliente → Productos → Resumen — modeled on v1's stepper, minus the payment step since v2 takes payment at delivery; reuses customers search + inline-create and catalog pricing; **required agreed payment method** at registry), order **queue** (status filter, payment/outstanding badges, assign/dispatch/cancel), an **assign** dialog (picks a driver's open inventory assignment, surfaces the backend oversell warnings + one-click "cargar balones faltantes" via `inventory.recordLoad`), a shared **detail** (lines, status timeline, payments, linked inventory tx, role+status-aware action bar), and a role-scoped **driver delivery view** (`/mis-entregas`, load+refresh) with dispatch/deliver/fail. Two user-approved backend follow-ons: **opened `GET /orders` to the delivery role** scoped to the driver's own assignments (+consolidated `field`/`anyStaff`), and a **nullable `orders.payment_method`** (migration `0007`). Gates green in both repos (frontend typecheck+build; backend typecheck+lint+**54 tests**+build; migration applied). Independent validation confirmed all 8 frontend criteria, no high/medium bugs. |
| 2026-06-08 | specs/customers + frontend (Piloto) | **`customers-crud` done (both tracks) → unblocks `orders`.** Backend: new `customers` vertical module: `customers` + `customer_debts` tables, `customer_debt_balance` view, migration `0005`; read API (`/api/v1/customers`) with name/phone search, debt-flagged list (monetary + empty-tank, no N+1), detail with empty-debt-by-type breakdown, create/PATCH with 409 dup-phone, operator+admin guard; **hardened the soft `customer_empty_debts.customer_id` into a real FK**; 4 lifecycle tests (48 total). **Frontend (Piloto):** `customers` vertical module mirroring `users`/`catalog` — list (debounced name/phone search + show-inactive + debt badges), single create/edit dialog (validation mirroring backend Zod; duplicate-phone 409 → phone-field error via a new `ConflictError` in `lib` + 409 mapping), detail with empty-tank (by type) + monetary debt summary; routes guarded operator+admin+developer; "Clientes" added to operator+admin nav. Monetary balance reads 0 until `orders` writes `customer_debts`. typecheck+build green; manual smoke left to the operator. Both tracks done → spec `done`. |
| 2026-06-06 | specs/inventory + frontend (Piloto) | **`inventory-foundation` frontend done → spec done.** New `inventory` vertical module (`src/modules/inventory/`): tabbed list (assignments + store availability) and per-assignment detail with server-sourced balances, seven ledger dialogs (open/sale/return/load/adjust/close/purchase) validating against the backend Zod schemas, discrepancies + full ledger views. Reuses `catalog` for tank-type/item names and the new `GET /catalog/store-assignments` driver picker. Centralized nav in `AppLayout` (`ROLE_NAV`, per-role) — removed the three duplicated `adminNav` arrays; operators get a drawer (Inicio + Inventario). Roles: admin + operator. typecheck + build green; manual smoke test left to the operator. |
| 2026-06-05 | specs/stores + frontend (Piloto) | **`stores-and-catalog` done** — frontend track shipped: new `catalog` vertical module (`src/modules/catalog/`) mirroring `users`; **tabbed** admin page (new hand-authored `components/ui/tabs/`) — Tipos de tanque / Artículos (list + admin create, client-side validation mirroring backend Zod) / read-only Tiendas, with a show-inactive (`?all=1`) toggle and per-list loading flags. Service hits `/catalog/{stores,tank-types,items}`. Also retheme: **burnt-orange flame primary** (`18 85% 40%`, white fg) replacing the stock blue that clashed with the dark-blue bg — on-brand for *la llama piloto*. Both tracks done → spec `done`. |
| 2026-06-05 | specs/inventory | **`inventory-foundation` backend done** — `src/modules/inventory/`: unified `tank_holders`/`item_holders` (location + driver-day), append-only tank/item/empty-debt ledgers, 3 SQL balance views, pure `kindToDelta` dispatch, three-state lifecycle (open → sale/return/adjust → close → carry) with paired opening/hand-back transfers, per-user/date reconstruction. 17 module tests; views smoke-verified on the dev DB. Depends on stores-and-catalog (also shipped today). |
| 2026-06-05 | specs/stores + eng | **`stores-and-catalog` backend done** — new `catalog` module: `stores`, `store_assignments`, `tank_types`, `inventory_items` (serial PKs, numeric prices, partial-unique active assignment), migration `0001`, idempotent `db:seed`, read API + admin create. Unblocks inventory. Also recorded **ADR-012** (explicit service composition over events) + **ADR-013** (location as inventory holder) and approved `inventory-foundation`. |
| 2026-06-04 | frontend (Piloto) | **Frontend named "Piloto"** (la llama piloto + the delivery driver) — wired into the document title, PWA manifest (`name`/`short_name`, env-overridable), the login screen, and the app-shell brand. App-shell nav moved from an inline top-bar to a left **menu drawer** (shadcn-vue `sheet`); top bar shows `Piloto · {page}` + user/logout. |
| 2026-06-04 | specs/users | `users-crud` **done** — frontend admin UI shipped: `users` vertical module (list with role/active filters + a guard-mirroring edit form for name/phone/role/active) built from scaffolded shadcn-vue components; `reka-ui` upgraded to 2.x and `LoginView`/`AppLayout` refactored onto the new components (ADR-011). Backend + frontend tracks both done. |
| 2026-06-04 | specs (all) | **Vault switched to `spec-layout: folder`.** Every feature spec converted to a folder with per-repo tracks. Merged `bootstrap/v2-skeleton` + `frontend-bootstrap/v2-skeleton` into one folder spec (backend + frontend tracks); removed the `frontend-bootstrap` category. `auth-foundation` (backend + frontend tracks, both done), `users-crud` (backend done, frontend not-started → overall in-progress), `inventory-foundation` (both not-started → draft). Added `vault-project` markers to lpg-frontend-vue and lpg-bot. |
| 2026-06-04 | specs/users | `users-crud` backend done — barebones `users` module on the auth-owned `users` table (no new schema): list + `?role=`/`?active=` filters, view one, partial PATCH of name/phone/role/active. Role assignment with developer-escalation + self-lockout guards. 5 lifecycle tests, all gates green. |
| 2026-05-08 | specs/bootstrap | `v2-skeleton` frontend track done — v1 archived to `legacy/` (230 files via git mv), fresh src/ with module-by-domain layout, shadcn-vue UI library, single-guard router, auth vertical slice + role-stub homes. 38 KB gzipped main bundle. |
| 2026-05-08 | eng + specs | Frontend documented in vault: `architecture.md` Frontend section filled in, `eng/frontend-bloat-analysis.md` added, the frontend skeleton drafted then implemented |
| 2026-05-08 | specs/auth | `auth-foundation` backend done — login, JWT, role middleware, invitations, BOOTSTRAP_TOKEN-gated developer creation, Redis logout blocklist (32 tests passing) |
| 2026-05-07 | eng | Vault initialized; backend v2 skeleton in place; v1 archived to `legacy/` |
