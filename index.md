---
project: lpg-store
status: active
spec-layout: folder
last-updated: 2026-06-15
---

# lpg-store

## Description

Backend, frontend dashboard, and chat bot for a small-city LPG (Liquefied Petroleum Gas) tank delivery business. The system tracks the chain of custody from inventory assignment to order fulfillment: which delivery user has which tanks each day, what was sold to whom, when each delivery happened, and how unpaid orders accumulate. **Primary value: traceability**, not analytics or accounting.

The product is pre-production. The backend is currently being rebuilt as v2 (clean reset, with v1 archived under `legacy/`). The frontend and bot are in earlier stages.

## Repositories

| Repo | Path | Stack | Role |
|------|------|-------|------|
| lpg-backend | /home/diegomh/dev/personal/freelance/lpg-store/lpg-backend | Node 22 / Express 5 / Drizzle / Postgres / Redis / TypeScript | backend (v2 in progress) |
| lpg-frontend-vue | /home/diegomh/dev/personal/freelance/lpg-store/lpg-frontend-vue | Vue 3 / Vite / Tailwind / Chart.js / Leaflet / PWA | dashboard frontend — **Piloto** |
| lpg-bot | /home/diegomh/dev/personal/freelance/lpg-store/lpg-bot | Node / Express / axios / commander | chat bot |

**Spec layout: `folder`.** Backend and frontend live in separate repos, so each feature spec is a folder (`specs/{category}/{slug}/`) with a shared `index.md` plus one track file per repo (`backend.md`, `frontend.md`, …). The overall spec is `done` only when every track is done.

## Domains

- [[eng/index]] — architecture, decisions, code patterns
- [[product/index]] — what the product does, user roles, requirements
- [[ops/index]] — deployment, CI/CD, runbooks
- [[specs/index]] — feature specs (draft, approved, in-progress, done)

## Active Work
- _None in flight._ Last shipped: [[specs/admin/org-management/index|org-management]] (both tracks done 2026-06-15).

## Recent Changes
| 2026-06-15 | specs/admin + frontend (Piloto) | **`org-management` frontend track done → spec `done` (both tracks); first `admin` spec complete.** New admin/developer-only **Organización** view (`/organizacion`) consolidating the scattered people-and-places surfaces into one location-centric board. A new `organization` vertical module **composes** `catalog` + `users` + `auth` (no duplication, per v2 conventions): the board renders one `Card` per store from the aggregate `GET /catalog/stores-with-assignments`, grouped **Operadores**/**Repartidores** with per-store **Asignar**/**Quitar** (assignment-id resolved from the catalog's active assignment list, since the aggregate omits it), **Editar** + **Nueva tienda** (reused `StoreFormDialog`/`StoreAssignmentDialog`, the latter gaining an optional `lockedStore` prop), and a show-inactive toggle. A **Usuarios** card lists all users with **Global** / **Sin tienda** flags, inline **Editar** (`UserEditDialog`), and **Invitar usuario** — a new dialog POSTing `/auth/register` (new `auth` `register` service+store action) that surfaces a **copyable `/invite/:token` link** + Lima-time expiry (no email infra in MVP). Old surfaces **replaced**: `AppLayout`/`AdminHome` Usuarios → Organización, Catálogo lost the *Tiendas*/*Asignaciones* tabs (keeps Tipos de tanque + Artículos), `/usuarios` → `/organizacion` redirect (`UsersListView` deleted; users store/service retained). The three reused dialogs emit `saved` so the board refreshes. Gates green (typecheck + build); independent validation met all 4 frontend criteria. Manual both-theme smoke left to the operator. |
| 2026-06-14 | specs/orders + frontend (Piloto) | **`orders-queue-history-search` frontend track done → spec `done` (both tracks).** `/pedidos` reshaped around its two jobs: an **Activos**/**Historial** segmented control (shared `Tabs`) — Activos (default) is the live, naturally-bounded queue (pending/assigned/in_transit, no date); Historial is the finished set (delivered/failed/cancelled) retrieved by a **Desde/Hasta** shared-`DatePicker` range (default today, no pager). Debounced (≥2-char) customer **search** box in both modes → `GET /orders?search=`, **accent-insensitive end-to-end** (consumes the backend `unaccent` fix; the order-creation picker gets “Maria”⇄“María” for free). Backend `status` is a single enum, so the “Todos”-within-mode view **fans out** one request per status in the store and merges by `createdAt`/`id` desc. Admin store switcher + Tienda column + ownership/transfer affordances all preserved; one batched watcher → single re-fetch per change; `onMounted` seeds Activos (never a no-status fetch of the unbounded history). Gates green (frontend typecheck + build); independent validation met all 5 criteria. Manual accent smoke left to the operator. |
| 2026-06-14 | specs/orders + frontend (Piloto) | **Follow-on to `orders-multi-location` (user-approved): operator location visibility & defaults.** Shared `catalog` foundation: `listStoreAssignments({userId})` + `myStores`/`fetchMyStores`. **`/pedidos/nuevo`** — owning-branch selector now shown to operators, scoped to their stores, pre-selected to the first (disabled if single); zero-store operator blocked with a clear message. **`/pedidos`** — operator scope line + Tienda column for multi-store operators. **`/inventario` Disponibilidad** — picker limited to the operator's stores, defaults to the first. Admin/dev unchanged. Gates green (frontend typecheck+build); independent validation met all 3 asks. _(Backend availability endpoint not yet role-scoped — client-side only; candidate follow-up.)_ |
| 2026-06-14 | specs/orders + both repos | **Follow-on to `orders-multi-location` (user-approved): branch-scoped the driver-assignment picker.** `GET /inventory/assignments` was unscoped — any caller saw every store's open days, so admin/the assign dialog could assign a driver from another branch. Backend: new `listAssignmentsForCaller(query, caller)` mirroring the orders-queue scoping — delivery → own days only; operator → their branch(es) (**multi-location aware** via `storeIdsForUser`); admin/dev global; `?storeId=` **intersected** with the caller's scope (tampered id → 0 rows, never a widening grant). 8 scoping tests → **70 total**. Frontend: `AssignDialog` takes the order's `storeId` and fetches the picker scoped to the order's location (both call sites pass it). Gates green both repos (backend typecheck+lint+build+70 tests; frontend typecheck+build). Documented in the `orders-multi-location` track notes (spec stays `done`). |
| 2026-06-14 | specs/stores + frontend (Piloto) | **`store-management` frontend track done → spec `done` (both tracks).** Admin write surface completed on the `catalog` module — no new routes/nav (Catálogo already admin-gated). New catalog store/service/types methods over the backend's `POST`/`PATCH /catalog/stores` + `/catalog/store-assignments`. **Tiendas** tab gains a `StoreFormDialog` (create + edit; name ≤120 / optional address ≤255 / phone ≤32 mirroring backend Zod; **Activa** Switch = activate/deactivate; show-inactive). New **Asignaciones** tab + `StoreAssignmentDialog`: user (reuses `useUsersStore`, filtered to active operator/delivery) + store pickers, list (who · rol · tienda · estado), per-row **Desactivar**. 409s (dup store name / dup active assignment) surface the backend Spanish message via the existing `ConflictError` mapping — no extra wiring. New stores flow straight into the orders store switcher / TransferDialog (shared `catalog.stores`). Independent validation: all 3 frontend criteria PASS. Gates green (typecheck + build, PWA precache 53 entries / 673.80 KiB). Manual both-theme smoke left to the operator. |
| 2026-06-14 | specs/orders + frontend (Piloto) | **`orders-multi-location` frontend track done → spec `done` (both tracks).** Additive changes to the `orders` module: `storeId`/`ownerId` on summaries; admin **branch switcher** (`?storeId=`, server-side filter) + **Tienda column** in the all-branches view; **`OwnershipBadge`** (Tú / Otro operador / Sin asignar) with assign/cancel/transfer **gated to mirror the backend** (owner-or-admin; assign auto-claims when unowned; field actions stay un-gated); **`TransferDialog`** (`POST /:id/transfer`, pre-dispatch, routes back to the queue after handoff); admin **required owning-branch selector** on create (operators default server-side); **409 concurrent-edit** re-sync (`resyncOnConflict` keeps the friendly message + re-fetches). Cross-repo follow-on (user-approved): added a `storeId` query param to backend `GET /orders` (intersected with the caller's visible stores) so the switcher filters server-side — backend 58 → **59 tests**. Gates green both repos (frontend typecheck+build; backend typecheck+lint+build). |
| 2026-06-14 | specs/orders + eng | **`orders-multi-location` backend track done.** Location + ownership dimension on orders: `orders.storeId` (NOT NULL) + `ownerId` (migration `0008`, back-fill to the seeded store); operator branch-scoping via `store_assignments` (cross-branch detail → 404; admin/dev global; delivery by assignment); owner-gated `assign`/`cancel`/`transfer` with auto-claim of unowned orders on assign; new `POST /:id/transfer` branch handoff (pre-dispatch; re-homes store, clears assignment, reverts to pending, releases owner). **Atomic transitions** — every state move is a compare-and-swap conditional `UPDATE … WHERE status=<expected>` (0 rows → 409), closing the lost-update assign race; deliver's flip is the first write in the tx (**ADR-016**). Payments branch-scoped (not owner-gated). 4 new tests → 58 total; typecheck+lint+build green; migration applied. Frontend track remains. |
| 2026-06-12 | specs/ui-design + frontend (Piloto) | **`design-system` done (frontend track) → spec `done`; first `ui-design` spec.** Full UI overhaul of Piloto for the low-tech-literacy operator/driver/admin audience. **Palette** iterated live with the owner to **"petrol + flame"**: deep petrol primary (white text both themes), cool petrol-tinted neutral surfaces, flame orange (`--brand`) reserved for the wordmark + focus ring; semantic `success/warning/info/destructive` fills plus text-grade `*-text` shades for dark-mode legibility — light + dark both first-class. **Foundation:** bundled self-hosted **Public Sans** (`@fontsource-variable/public-sans`, PWA-precached), global type-scale floor (xs→14px, sm→15px), `h-11 md:h-10` ≥44px touch sizing, comfortable table density. **Theme system:** `src/lib/theme.ts` (vueuse `useColorMode`, key `piloto-theme`, system default), FOUC guard + theme-color metas in `index.html`, Sun/Moon toggle in shell + login; removed the hardcoded dark default. **Shared chrome:** `components/app/{PageHeader,Spinner,EmptyState}` + `lib/format.formatMoney` (killed the per-module `money()` dupes); `TableEmpty`+`Spinner` for every table state. **Retrofit** of the app shell + all 7 modules with the canonical status→badge mapping; **DeliveryListView** rebuilt as a phone-first **Card list** (full address, `tel:` link, full-width actions). **`eng/design-system.md`** written as the canonical convention doc + hard-rule checklist for all future UI work. Independent validation: **all 10 acceptance criteria PASS**, zero raw palette classes. Gates green (typecheck + build, precache 656 KiB). Manual both-theme smoke left to the operator. |
| Date | Domain | Summary |
|------|--------|---------|
| 2026-06-10 | specs/orders + frontend (Piloto) | **`orders-foundation` done (both tracks) → spec `done`; the v2 core business workflow is complete.** Frontend `orders` vertical module (Piloto): operator entry **wizard** (Cliente → Productos → Resumen — modeled on v1's stepper, minus the payment step since v2 takes payment at delivery; reuses customers search + inline-create and catalog pricing; **required agreed payment method** at registry), order **queue** (status filter, payment/outstanding badges, assign/dispatch/cancel), an **assign** dialog (picks a driver's open inventory assignment, surfaces the backend oversell warnings + one-click "cargar balones faltantes" via `inventory.recordLoad`), a shared **detail** (lines, status timeline, payments, linked inventory tx, role+status-aware action bar), and a role-scoped **driver delivery view** (`/mis-entregas`, load+refresh) with dispatch/deliver/fail. Two user-approved backend follow-ons: **opened `GET /orders` to the delivery role** scoped to the driver's own assignments (+consolidated `field`/`anyStaff`), and a **nullable `orders.payment_method`** (migration `0007`). Gates green in both repos (frontend typecheck+build; backend typecheck+lint+**54 tests**+build; migration applied). Independent validation confirmed all 8 frontend criteria, no high/medium bugs. |
| 2026-06-08 | specs/customers + frontend (Piloto) | **`customers-crud` done (both tracks) → unblocks `orders`.** Backend: new `customers` vertical module: `customers` + `customer_debts` tables, `customer_debt_balance` view, migration `0005`; read API (`/api/v1/customers`) with name/phone search, debt-flagged list (monetary + empty-tank, no N+1), detail with empty-debt-by-type breakdown, create/PATCH with 409 dup-phone, operator+admin guard; **hardened the soft `customer_empty_debts.customer_id` into a real FK**; 4 lifecycle tests (48 total). **Frontend (Piloto):** `customers` vertical module mirroring `users`/`catalog` — list (debounced name/phone search + show-inactive + debt badges), single create/edit dialog (validation mirroring backend Zod; duplicate-phone 409 → phone-field error via a new `ConflictError` in `lib` + 409 mapping), detail with empty-tank (by type) + monetary debt summary; routes guarded operator+admin+developer; "Clientes" added to operator+admin nav. Monetary balance reads 0 until `orders` writes `customer_debts`. typecheck+build green; manual smoke left to the operator. Both tracks done → spec `done`. |
| 2026-06-06 | specs/inventory + frontend (Piloto) | **`inventory-foundation` frontend done → spec done.** New `inventory` vertical module (`src/modules/inventory/`): tabbed list (assignments + store availability) and per-assignment detail with server-sourced balances, seven ledger dialogs (open/sale/return/load/adjust/close/purchase) validating against the backend Zod schemas, discrepancies + full ledger views. Reuses `catalog` for tank-type/item names and the new `GET /catalog/store-assignments` driver picker. Centralized nav in `AppLayout` (`ROLE_NAV`, per-role) — removed the three duplicated `adminNav` arrays; operators get a drawer (Inicio + Inventario). Roles: admin + operator. typecheck + build green; manual smoke test left to the operator. |
| 2026-06-05 | specs/stores + frontend (Piloto) | **`stores-and-catalog` done** — frontend track shipped: new `catalog` vertical module (`src/modules/catalog/`) mirroring `users`; **tabbed** admin page (new hand-authored `components/ui/tabs/`) — Tipos de tanque / Artículos (list + admin create, client-side validation mirroring backend Zod) / read-only Tiendas, with a show-inactive (`?all=1`) toggle and per-list loading flags. Service hits `/catalog/{stores,tank-types,items}`. Also retheme: **burnt-orange flame primary** (`18 85% 40%`, white fg) replacing the stock blue that clashed with the dark-blue bg — on-brand for *la llama piloto*. Both tracks done → spec `done`. |
| 2026-06-05 | specs/inventory | **`inventory-foundation` backend done** — `src/modules/inventory/`: unified `tank_holders`/`item_holders` (location + driver-day), append-only tank/item/empty-debt ledgers, 3 SQL balance views, pure `kindToDelta` dispatch, three-state lifecycle (open → sale/return/adjust → close → carry) with paired opening/hand-back transfers, per-user/date reconstruction. 17 module tests; views smoke-verified on the dev DB. Depends on stores-and-catalog (also shipped today). |
| 2026-06-05 | specs/stores + eng | **`stores-and-catalog` backend done** — new `catalog` module: `stores`, `store_assignments`, `tank_types`, `inventory_items` (serial PKs, numeric prices, partial-unique active assignment), migration `0001`, idempotent `db:seed`, read API + admin create. Unblocks inventory. Also recorded **ADR-012** (explicit service composition over events) + **ADR-013** (location as inventory holder) and approved `inventory-foundation`. |
| 2026-06-04 | frontend (Piloto) | **Frontend named "Piloto"** (la llama piloto + the delivery driver) — wired into the document title, PWA manifest (`name`/`short_name`, env-overridable), the login screen, and the app-shell brand. App-shell nav moved from an inline top-bar to a left **menu drawer** (shadcn-vue `sheet`); top bar shows `Piloto · {page}` + user/logout. |
| 2026-06-04 | specs/users | `users-crud` **done** — frontend admin UI shipped: `users` vertical module (list with role/active filters + a guard-mirroring edit form for name/phone/role/active) built from scaffolded shadcn-vue components; `reka-ui` upgraded to 2.x and `LoginView`/`AppLayout` refactored onto the new components (ADR-011). Backend + frontend tracks both done. |
| 2026-06-04 | specs (all) | **Vault switched to `spec-layout: folder`.** Every feature spec converted to a folder with per-repo tracks. Merged `bootstrap/v2-skeleton` + `frontend-bootstrap/v2-skeleton` into one folder spec (backend + frontend tracks); removed the `frontend-bootstrap` category. `auth-foundation` (backend + frontend tracks, both done), `users-crud` (backend done, frontend not-started → overall in-progress), `inventory-foundation` (both not-started → draft). Added `vault-project` markers to lpg-frontend-vue and lpg-bot. |
| 2026-06-04 | specs/users | `users-crud` backend done — barebones `users` module on the auth-owned `users` table (no new schema): list + `?role=`/`?active=` filters, view one, partial PATCH of name/phone/role/active. Role assignment with developer-escalation + self-lockout guards. 5 lifecycle tests, all gates green. |
| 2026-05-08 | specs/bootstrap | `v2-skeleton` frontend track done — v1 archived to `legacy/` (230 files via git mv), fresh src/ with module-by-domain layout, shadcn-vue UI library, single-guard router, auth vertical slice + role-stub homes. 38 KB gzipped main bundle. |
| 2026-05-08 | eng + specs | Frontend documented in vault: `architecture.md` Frontend section filled in, `eng/frontend-bloat-analysis.md` added, the frontend skeleton drafted then implemented |
| 2026-05-08 | specs/auth | `auth-foundation` backend done — login, JWT, role middleware, invitations, BOOTSTRAP_TOKEN-gated developer creation, Redis logout blocklist (32 tests passing) |
| 2026-05-07 | eng | Vault initialized; backend v2 skeleton in place; v1 archived to `legacy/` |
